version: '3'

services:
  shearreinforcement:
    build: 
        context: ../
        dockerfile: ./build/Dockerfile
        args:
          NODE_VERSION: 18.17.0
          PROJECT_NAME: ShearReinforcement.Web
    ports:
    - "5000:80"
    
